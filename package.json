{
  "name": "pi-delegate",
  "version": "0.1.0",
  "description": "Minimal subagent delegation for Pi agents. Spawns isolated Pi subprocesses and returns curated results.",
  "type": "module",
  "license": "MIT",
  "author": "Juanje Ojeda",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juanje/pi-delegate.git"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-coding-agent",
    "delegate",
    "subagent",
    "context-isolation"
  ],
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "vitest run",
    "check": "tsc --noEmit && biome check . && vitest run"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.0"
  }
}
